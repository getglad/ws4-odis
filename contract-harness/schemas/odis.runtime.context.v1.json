{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://apf.local/schemas/odis.runtime.context.v1.json",
  "title": "odis.runtime.context.v1",
  "description": "Trusted runtime context envelope. Mirrors APF §6.1; populated by the substrate, consumed by the MCP adapter when building an authz request. Caller-supplied subject fields are discarded by the substrate; nothing in this envelope is sourced from the agent's call payload.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "correlation_id",
    "bundle_id",
    "bundle_version",
    "trust_root_id",
    "policy_digest",
    "sponsor",
    "agent",
    "task_intent",
    "target_resource",
    "issued_at",
    "phase"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "odis.v1",
      "description": "Distinct from apf.*.v1; refusing apf.*.v1 prevents consumers mistaking ODIS Contract Harness envelopes for APF Core artifacts."
    },
    "correlation_id": {
      "type": "string",
      "format": "uuid",
      "description": "Threaded through every envelope and audit event for one logical agent action (APF-L3-07)."
    },
    "bundle_id": {
      "type": "string",
      "const": "odis-fixture-bundle",
      "description": "Stub policy metadata; not a real signed bundle id."
    },
    "bundle_version": {
      "type": "string",
      "const": "0.0.0-odis-harness"
    },
    "trust_root_id": {
      "type": "string",
      "const": "odis-fixture-trust-root"
    },
    "policy_digest": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "SHA-256 hex of the canonicalized policy bundle (the policy_digest binding)."
    },
    "sponsor": {
      "$ref": "#/$defs/Identity",
      "description": "Source: SponsorIdentityProvider (apf-openshell-containment), not the agent payload."
    },
    "agent": {
      "$ref": "#/$defs/Identity",
      "description": "Source: WorkloadIdentityProvider (apf-openshell-containment), not the agent payload."
    },
    "task_intent": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable description of what the agent is attempting; informational, not policy-evaluable."
    },
    "target_resource": {
      "$ref": "#/$defs/ResourceRef"
    },
    "issued_at": {
      "type": "string",
      "format": "date-time"
    },
    "phase": {
      "type": "string",
      "const": "odis-harness"
    }
  },
  "$defs": {
    "Identity": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "type"],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "type": {
          "type": "string",
          "minLength": 1,
          "description": "Identity scheme: entra_oidc, spiffe, k8s_sat, hw_attested, fixture_workload_identity, etc."
        }
      }
    },
    "ResourceRef": {
      "type": "object",
      "additionalProperties": false,
      "required": ["resource_family"],
      "properties": {
        "resource_family": {
          "type": "string",
          "minLength": 1,
          "description": "Key into the signed bundle's per-family routing (e.g., 'jira')."
        },
        "instance_id": {
          "type": "string",
          "minLength": 1,
          "description": "Provider-specific instance handle (e.g., a Jira issue key like 'APF-123')."
        }
      }
    }
  }
}
